# 2026-09-22 工作记录

## 中证红利（000922）股息率信号每日检查（automation，10:03）

- 记录日期 2026-09-22；指数估值日期 2026-09-21（前一交易日，正常）；10年国债收益率日期 2026-09-21；历史分位数据日期未取得。
- 股息率2 = 4.21%；10年国债 = 1.6829%；利差 = 2.5271 个百分点；股息率/国债 = 2.50 倍；年内收益率 0.04%；年内最大回撤 −15.96%。
- 三类评级：历史分位点 待验证 / 绝对股息率 C / 相对债券收益率 C。
- 脚本综合结论：非「加大买入区间」。问题页「重点买入区间」：未触发（2.50 倍 < 3 倍；分位待验证）。
- 首轮运行因中证指数估值接口 SSL EOF 报错整体失败、未写文件；原样重跑成功。
- 理杏仁公开页面 403、WebFetch ForbiddenError；网页核验仅有 PE/PB 十年分位与利差分位，口径不符 → 未传 `--llm-lixinger-*`，保留待验证。
- 文件：`sources/automations/中证红利信号/中证红利每日信号.xlsx`（含表头 83 行，新增 2026-09-22 行）、`最新信号.md`、`中证红利年度表现.json`（19 年，lastDate 2026-09-21）；根 `log.md` 追加 audit 记录；`index.md` 不变。

- 16:00 BBXM 每日汇总自动化（冰冰小美）：2026-09-22 空结果（0 原帖，DOM 核验最新帖 09-18 17:05）；空风险清理后更新器返回 no_risk，日期目录已按主提示词删除；根 log.md 已追加 audit 记录；详见自动化记忆文件。

- 17:10 BBXM 每日汇总任务规则变更：主提示词 `.agents/automations/bbxm_daliy_brief.md` 第四节风险分析由「使用 bbxm-expert 路径 + risk-node-identification 规则」改为**强制实际调用 `.agents/skills/bbxm-risk-identification/`**（先读 SKILL/workflow/template/handoff-contract，走五步识别）。边界：技能仅作方法与判定来源，自动任务不写 tools/a-share-market-dashboard/data/Risk/、不生成 HTML、不另跑 information-processing（当日原帖集合等价 feed 上游）；空结果日不加载技能。4.2 明确 identification_status 与 R/W 等级的对应（仅「已支持」可定 R/W，候选/证据不足归「待验证」）。4.3 新增 skill_call 调用痕迹字段；summary 与第八节报告同步新增技能调用状态项。已实测含 skill_call 的空结果样例通过更新器校验。根 log.md 追加 refactor 记录。

- 17:30 手动补抓 2026-09-18（闭环长期挂起的 09-18 空目录待办）：`sources/automations/BBXM每日汇总/2026/09/2026-09-18/`。候选 10 → 保存 7 篇（4 中文 + 3 英文）。关键产出与经验：
  - **09-18 确实有内容**，此前"9 月 18 日归档目录没有新的有效文章"的结论错误（当时只抽样首页 DOM 首屏）。已在根 log.md 补勘误。
  - 删除误存他人帖 `131700_…_40978055.md`（作者 仙居杨梅最初的梦，作者页 /5418424084/）；文件名高位截断易致 ID 比对失败，须用 8 位前缀匹配。
  - **雪球图标字体是评论噪声主因**：`\ue64b`=赞图标、`\ue633`=评论图标，与数字粘连成 `\ue64b9`/`\ue6332`，或单独成行只剩 `1`/`9`/`2`。清洗须先剥离私用区 `[\ue600-\ue7ff]` 再判噪声，否则孤立数字会被当成正文收入作者评论。
  - 详情页正文只取 `.article__bd` / `.status-content`，否则会带入导航/热门话题/热股榜/基金榜外壳。
  - **专栏页与社区页是同一篇文章的两个入口**：409824691（「预演」17:05 社区页）与 409823838（16:59 专栏页）同源；社区页「正文」位置显示的是他人评论（钱老板赚钱 回复@偷窃贼），作者原文只在专栏页。社区页「交流帖+作者补充长文」模式要优先查专栏页正文。处置：409823838 为保留载体（回填专栏正文 600 字并重排为 12 段），409824691 标注重复载体仅作来源索引，state 记 `duplicate_of`，`content_count=6`（条目 7）。
  - 专栏页正文按标点逐行输出（`根据历史推演\n，\n\n…`），重排规则：先去所有硬换行拼成连续文本，再按句末标点 `。！？` 分段、每段聚合 ≥40 字；结果与社区页原始保存版本逐句一致，可作独立验证。
  - 风险分析按新规则实际调用 `bbxm-risk-identification` 五步路径，覆盖 7/7、未解决 0，合格 R/W = 0，全部归入「待验证」（候选 4 / 证据不足 3 / 重复载体 1）。根因：作者当日只给风险源头与交易结构的定性判断，无「已发生的不利结果」，缺可比窗口与可观察负反馈。Excel 更新器返回 `no_risk`。
  - 已生成 `summary.md`（总观点/解析今天文章的观点/当日内容汇总/风险提示判定）；方向偏好（偏投资、避投机）≠ 买入信号，当日无买卖信号，未生成 `操作.md`。
- 环境提示（本会话持续）：bash 的 PATH 损坏（dirname/cd/ls/cat 均 not found），PowerShell 直出 stdout 也常为空；可靠路径是 PowerShell 写 `.NET` API，输出重定向到文件后用 Read 工具读取。

## BBXM 2026-09-22 当日帖子整理（automation 续跑，18:00–18:30）

- 背景：16:00 定时轮空结果属实（当日 3 帖发布于 16:27／16:44／17:53，均晚于 16:00）；17:34 手动重跑仍 0 候选则是**冷启动渲染竞态缺陷**：冷启动 Chrome 的 `document.readyState` 到达 `interactive` 即进入提取，主页时间线 AJAX 未渲染完，HOME_SCRIPT 静默返回空 JSON（exit 0），与真实无帖日不可区分（16:00 轮与 09-21 的 `scrollHeight of null` 是另一种冷启动竞态，表现形式不同）。
- 技能修复：`extractHomepageCandidates` 增加 `emptyRetryDelayMs`（默认 3000ms）参数——首轮 0 候选时等待后重跑一次主页提取再报空；新增 2 项回归测试（57 项全过），同步 WorkBuddy 副本（3 文件 SHA-256 一致），记入技能 `log.md`。修复后 17:57 重跑成功。
- **PowerShell `Start-Process` 失败根因**（本会话新发现）：宿主环境块同时含 `PATH` 与 `Path` 两个键，Start-Process 构建大小写不敏感字典时抛 ArgumentException「已添加项。字典中的关键字:"PATH"」。这也是此前 PATH 异常的来源之一。可靠方式：bash 内以绝对路径运行 node/python（bash 内建 echo/export 可用），输出重定向到文件后用 Read 读。
- 保存与清洗：3 篇原帖（16:27 图片帖 410215494／16:44 观点帖 410217703／17:53 热榜帖 410225664）。图片帖正文写事实性说明；16:44 帖重排 9 段 517 字；评论时间正则扩展支持相对时间戳（N秒/分钟/小时前、今天 HH:MM）——09-18 版只认绝对时间会漏当日相对时间戳评论；16:27 帖 16:46 作者长评与 16:44 帖正文一致，按内容指纹标注「同文」不重复计次；17:53 帖标签由「市场，行业」修正为「市场」（页面热门话题残留污染）。
- 风险分析：按 `.agents/skills/bbxm-risk-identification/`（项目权威版 v1.0.2 五步识别；WorkBuddy 副本技能是旧版方向判断版，不可用作依据）逐帖五步识别，覆盖 3/3、未解决 0；合格 R/W = 0，全部「待验证」（候选 2：16:44 帖投机生态传导、16:27 帖评论融券做空机制；证据不足 1：17:53 帖热榜现象）。更新器 `no_risk`。
- 知识库检索印证（§6.2）：View「A股投机产业链根源在交易制度与融资市场定位的判断框架」、Reasoning「A股投机产业链如何由做多制度与低违规成本形成」、Timeline「近年来更新脉络」（摔杯为号=作者「冰点摔杯」择时信号）。
- 产出：`sources/automations/BBXM每日汇总/2026/09/2026-09-22/` 下 3 篇原帖 + `summary.md` + `state.json` + `task.log` + `processing/`；根 `log.md` 追加 16:00 轮勘误与 ingest 正条目。当日作者「难以抉择」、无明确买卖信号，未生成 `操作.md`。
